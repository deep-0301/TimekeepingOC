import { InfoOutlined } from "./icons";

/**
 * The "more about this" glyph. Kept as its own module so the help components
 * name what they mean rather than which Material icon happens to say it.
 */
export default function InfoIcon() {
  return <InfoOutlined />;
}
